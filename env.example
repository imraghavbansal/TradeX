# Copy this file to .env.local in the same directory and fill values

# === Required ===
# Finnhub API key for search and market data server actions
NEXT_PUBLIC_FINNHUB_API_KEY = d3utga9r01qil4arnfl0d3utga9r01qil4arnflg

# === Optional (enable persistent watchlist) ===
# MongoDB connection string. If omitted, watchlist can be mocked/disabled.
MONGODB_URI=

# === Optional (misc) ===
# Useful if you need absolute URLs in emails/webhooks
NEXT_PUBLIC_SITE_URL=
